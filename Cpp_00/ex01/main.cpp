#include "Phonebook.hpp"

int main()
{
    std::string input;
    int i;
    while (1)
    {
        std::cout << "INPUT : ";
        std::getline(std::cin, input);
        if (input == "EXIT")
            break;
        else if (input == "ADD")
        {
            if (i < 7)
            {

            }
        }
    }
    return 0;
}